#include <bits/stdc++.h>
typedef unsigned long long ll;

using namespace std;

int main()
{
    ifstream inp("PTIT.in");
    ofstream out("PTIT.out");
    string line;
    while (getline(inp, line))
    {
        out << line << "\n";
        
    }
    inp.close();
    out.close();
    return 0;
}