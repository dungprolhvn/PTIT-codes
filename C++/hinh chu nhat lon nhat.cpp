#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, n, m; cin >> t;
    while (t--)
    {
        int mat[15][15];
        cin >> n >> m;
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < m; j++)
            {
                cin >> mat[i][j];
            }
        }
        int rs = 0;
        vector<int> v;
        for ()
    }
    return 0;
}